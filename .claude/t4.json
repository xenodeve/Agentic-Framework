{
  "t4": true,
  "verify": "C:/Users/xenod/.bun/bin/bun.exe run lint && C:/Users/xenod/.bun/bin/bun.exe run typecheck && C:/Users/xenod/.bun/bin/bun.exe run build",
  "autoMerge": false,
  "requireGreenCI": false,
  "note": "Marker that flags this as a T4 repo — the hooks fire only when this file is present. \"verify\" is armed to the fast subset (lint + typecheck + build; the repo has no unit test runner yet) and is kept in sync with .github/workflows/t4-verify.yml — t4-gate RUNS it before `gh pr merge` and blocks on failure. \"requireGreenCI\" is FALSE on purpose: the account is billing-locked, so CI cannot run at all and `gh pr checks` reports non-zero for no checks exactly as for failing — true would deny every merge forever. Flip it true (and create the ruleset) when billing is restored; tracked in the open-work ledger. \"autoMerge\"/\"afk\" stay false until an unattended run is explicitly authorized."
}
