{
  "t4": true,
  "verify": "C:/Users/xenod/.bun/bin/bun.exe run lint && C:/Users/xenod/.bun/bin/bun.exe run typecheck && C:/Users/xenod/.bun/bin/bun.exe run build",
  "autoMerge": false,
  "requireGreenCI": false,
  "note": "Marker that flags this as a T4 repo — the hooks fire only when this file is present. \"verify\" is armed to the fast subset (lint + typecheck + build; the repo has no unit test runner yet) and is kept in sync with .github/workflows/t4-verify.yml — t4-gate RUNS it before `gh pr merge` and blocks on failure. \"requireGreenCI\" is off until the first CI run has landed and the ruleset (or its fallback) is known — flip it true only if required checks cannot be made. \"autoMerge\"/\"afk\" stay false until an unattended run is explicitly authorized."
}
